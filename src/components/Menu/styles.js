import styled from "styled-components"

export const Container = styled.div`
  > .menu {
    img {
      width: 2.4rem;
      height: 1.8rem;
    }

    text-decoration: none;
    background: none;
    border: none;
  }
`
